{
  "name": "svelte-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "prettier --write . && eslint src/** ",
    "lint-fix": "prettier --write . && eslint src/** --fix"
  },
  "devDependencies": {
    "eslint-config-airbnb-prettier-import": "^1.0.0",
    "svelte": "^3.0.0"
  }
}
